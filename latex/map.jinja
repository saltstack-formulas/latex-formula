{% set latex = salt['grains.filter_by']({
    'Debian': {
        'pkgs': ['texlive-latex-recommended', 'texlive-latex-extra'],
        'beamer': 'latex-beamer',
    },
    'RedHat': {
        'pkgs': ['texlive-latex'],
    },
}, merge=salt['pillar.get']('latex:lookup')) %}
